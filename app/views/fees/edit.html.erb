<div class="row mb-5">
  <div class="col">
    <h1>Editing Fee <%= @fee.id %></h1>
  </div>
  <div class="col text-end">
    <%= link_to '&laquo Cancel'.html_safe, @fee, class: 'btn btn-outline-primary' %>
  </div>
</div>

<div class="form-container">
  <%= render 'form', fee: @fee, item: nil, standard_fees: @standard_fees  %>
</div>
