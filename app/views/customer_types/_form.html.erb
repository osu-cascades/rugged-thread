<%= form_with(model: customer_type) do |form| %>
  <% if customer_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(customer_type.errors.count, "error") %> prohibited this customer type from being saved:</h2>

      <ul>
        <% customer_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="actions">
    <%= form.submit 'Save', class: 'form-control btn btn-success' %>
  </div>
<% end %>
