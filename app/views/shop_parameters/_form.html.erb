<%= form_with(model: shop_parameter) do |form| %>
  <% if shop_parameter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(shop_parameter.errors.count, "error") %> prohibited this shop parameter from being saved:</h2>

      <ul>
        <% shop_parameter.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control'%>
  </div>

  <div class="mb-3">
    <%= form.label :amount, class: 'form-label' %>
    <%= form.number_field :amount, class: 'form-control'%>
  </div>

  <div class="actions">
    <%= form.submit 'Save', class: 'form-control btn btn-success' %>
  </div>
<% end %>

