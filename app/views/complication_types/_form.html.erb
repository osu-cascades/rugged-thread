<%= form_with(model: complication_type) do |form| %>
  <% if complication_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(complication_type.errors.count, "error") %> prohibited this complication_type from being saved:</h2>

      <ul>
        <% complication_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
