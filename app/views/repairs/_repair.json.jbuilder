json.extract! repair, :id, :charge, :quote, :shop_rate, :time_total, :date, :time, :item_number, :number, :invoice_item_id, :repair_type_id, :technician_id, :notes, :created_at, :updated_at
json.url repair_url(repair, format: :json)
