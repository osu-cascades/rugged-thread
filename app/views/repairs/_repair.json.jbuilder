json.extract! repair, :id, :charge, :quote, :shop_rate, :time_total, :date, :time, :item_number, :number, :technician_name, :notes, :created_at, :updated_at
json.url repair_url(repair, format: :json)
